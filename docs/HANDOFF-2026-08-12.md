# Motto Session Handoff — 2026-08-12

> 供下个 session 续行 + 目验。全部改动已落 `motto/main`，工作树干净。
> 本文件为新 session 导航；目验完成、无遗留后可归档至 `docs/archive/` 或删除。

## 一、本次会话完成（commit 时间序，最旧在前）

```
806b11796 feat(motto): 层二 卡片用法 system prompt 注入（方向纠正后已回退，见下）
c9d912479 revert: 回退层二——不在 harness 层做 agentic 约束，只接住模型自然行为
30042d846 feat(motto): 、、、卡片保真演进(方案A)——带标注围栏接住(标注=粗体头)、
           内容每行一表格行保缩进保空行(零core)
a1c368765 docs: 、、、卡片帧调研笔记(7家开源对比+方案A裁定依据)
8f1e90d61 feat(tui): 、、、卡片轻帧——<!--motto-card--> 标记使投影卡片去行间分隔线
           (自然表格不变,登记 PATCHES.json tui-4-s1-card-light-frame)
ccf86f3ef docs: PATCHES.json 修正 commit 字段为实际哈希
311fa3b5b merge: feat/card-light-frame → motto/main
04e0618ce docs: INDEX 召回登记(两份调研笔记入架构索引)
e6af3794d + 943e67312 + 395ac7fce + 984808339
            merge: review-flow A1 收敛化(自定义工具成功目行 + !! bash 有界预览5行,
            PATCHES.json review-flow-a1-tool-index-all)
```

## 二、当前能力状态（TUI 已实现）

| 能力 | 落点 |
|---|---|
| `、、～` 卡片（裸/带标注 bash/txt 等）| `packages/motto/extensions/motto/cards.ts`：标注→粗体头、每行一表格行、保缩进/空行、marker `<!--motto-card-->` |
| 卡片轻帧（去行间分隔线）| `packages/tui/src/components/markdown.ts` `cardFrameMode`：仅外框+表头线；自然表格通行方案不变 |
| review-flow A1 收敛化 | `tool-execution.ts`（自定义工具成功→目行）；`bash-execution.ts`（`!!` bash 整卡+5行tail预览）|
| 既有 | 牌记/footer/TPS、标题守护、多级标题投影、thinking 三态（T2）、S3 工具目行、S1/S2 布局、composer dock（上游）|

**PATCHES.json 现有 9 条 applied**：tui-1-s1/s2/s3、tui-2-t1/t2/t3、motto-rebrand-1、tui-4-s1-card-light-frame、review-flow-a1-tool-index-all。

## 三、新 session 待办（续行 + 目验）

### 1. 目验（⚠️ 必须完整重启 motto，/reload 只重载扩展、不重载核心）
重启后验证，建议一张卡测：
```
、、、 bash
git status --short
  M packages/motto/extensions/motto/cards.ts
git diff --stat
  2 files changed, 35 insertions(+)
、、、
```
预期：渲染为**轻帧单盒**——粗体头 bash、无 `<!--motto-card-->` 字面、无行间 `├─┤`（仅表头线）。
另验：自定义工具成功→单行 dim 目行；`!!` bash→整卡+5行tail预览；失败整卡强显。

### 2. 读屏
`/computer-use approve`（重启会重置 gate）→ `cu_see`/`cu_image` 读真实渲染。

### 3. review-flow 后续（观察期纪律：**不以调研代替使用**）
- **方案 B**（三态折叠+类型化预算+fold键，调研 `docs/architecture/TUI-REVIEW-FLOW-RESEARCH.md`）：触发 = 使用中记录到「想看输出又要折叠」真实摩擦。
- **方案 C**（diff-only 改笔面，review-flow v2）：触发 = 改笔不可审真实摩擦。

## 四、已知坑（避免重复踩）

1. `/reload` ≠ 重启：核心（tui/coding-agent dist）改动必须完整重启才生效。
2. reload/重启重置 computer-use gate → 每次需 `/computer-use approve`。
3. PATCHES.json `commit` 字段自引用（patch 内引用自身哈希）→ 先例：后续 fix commit 修正。
4. 层二已回退，原则确立：**不在 harness 层做 agentic 约束，TUI 只接住模型自然行为**——模型自然产出的卡片/表格/代码块，由 TUI 适配而非教模型说话。

## 五、关键文件速查

- 卡片投影：`packages/motto/extensions/motto/cards.ts`（+ tests/cards.test.mjs）
- 轻帧核心：`packages/tui/src/components/markdown.ts`（cardFrameMode）
- 工具目行：`packages/coding-agent/src/modes/interactive/components/tool-execution.ts`
- `!!` bash：`packages/coding-agent/src/modes/interactive/components/bash-execution.ts`（PREVIEW_LINES=5）
- 制度：`docs/maintenance/PATCHES.json`、`docs/INDEX.md`
- 调研：`docs/architecture/TUI-REVIEW-FLOW-RESEARCH.md`、`docs/architecture/TUI-CARD-FRAME-RESEARCH.md`
- 宣言正典：`docs/MOTTO-PHILOSOPHY.md`（8 核心取舍 + 吸纳理念）

## 六、本 session 未做（留待触发/裁定）

- 卡片：流式实时成卡、user 消息卡片（按使用摩擦触发）
- review-flow B/C（观察期触发）
- 主题边框粗细微调（用户曾提「边框线略微过重」，轻帧已缓解主体观感，外框粗细可后续调）
