# Motto 仓上线省视记录 — remote 接线 · CI 首跑 · 命名同步（2026-08-08）

> 日期：2026-08-08
> 依据：工单「Motto 仓上线——remote 接线 + 本地命名同步 + 口径改齐（单写者）」
> 触发：环境触发——remote `https://github.com/lesPrivilege/Motto.git` 成立
> 前置：push 前置核查一票项通过（隐私与凭据复扫、workflow 自动生效确认）

## 一、push 前置核查结论

1. **隐私与凭据复扫**：全仓 key/token 形状扫描干净（唯一命中为 `review-flow.test.mjs` 内 JWT 形状测试 fixture，非凭据）。
   - 个人路径 `/Users/lesprivilege` 仅存于两处**历史文献**：`reports/PACK-COMPUTER-USE-1-live.log`（验收现场日志）与 `docs/decisions/review-flow-eval.md`（决策记录）。裁定：**可公开，不改史**——均为验收/决策原样，无凭据，按「不改史，注补史」原则处理，另按省并悬置裁定补编者注两条。
2. **workflow 自动生效确认**：`.github/workflows/ci.yml`（`on: push: branches:[main]`）+ `upstream-pin-verify.yml`（每周一 cron + workflow_dispatch）随 push 即生效，无需额外启用动作。

## 二、CI 首跑记录（ROADMAP「CI 真实 runner」保留项自然解冻）

| 序 | 触发 commit | run | 结果 | 结论 |
|---|---|---|---|---|
| 首跑 | `4d201c1` 省并收官（remote 接线首 push） | `31252433285` | **红** | governance PASS；regression FAIL——`regression.sh` 的 drift-check 在无部署位的 fresh runner 上必然报警（`/Users/runner/.pi/agent/extensions/*` 缺失）。属 **runner 环境差异**，非门槛问题。 |
| 复跑 | `b06108b`（修 `regression.sh`：无部署位即 SKIP drift-check，与 governance 同策略） | `31252539913` | **绿** | governance PASS + regression PASS（9 passed, 0 failed；drift SKIP）。 |
| 再跑 | `c4b5884`（README 补 clone URL） | `31252653211` | **绿** | governance PASS + regression PASS。 |

**处置原则**：红只修 runner 环境差异，不改任何门槛。修正为给 `scripts/regression.sh` 加与 `ci-checks.sh governance` 一致的部署位存在性守卫（`PI_CODING_AGENT_DIR` 下 `extensions` 存在才跑 drift-check，否则 SKIP）。

## 三、命名同步与口径改齐

1. **目录改名**：`~/Projects/motto-extensions` → `~/Projects/Motto`（git 状态、remote 均完好）。
2. **口径改齐**（现行文档，单独 commit `67bfad2`）：README / AGENTS / MAINTENANCE / MIGRATION / MOTTO 中仓名与 clone URL 统一为 `Motto`；`extensions/motto/README.md` 安装行 `~/Projects/motto-extensions` → `~/Projects/Motto`；`c4b5884` 补 README 快速开始 clone URL。
3. **部署位同步**（`~/.pi/agent`，commit `d0a8c11`）：`check.sh`（REPO=）、`MOTTO.md` 指针、`.gitignore` 注释改齐。`notes/archive/` 历史档案不改史。
4. **不改史，注补史**：历史文献（reports/decisions/reviews/usage-log/archive）原文不动；按省并悬置裁定补两条编者注（PACK-1「默认不启用」措辞、review-flow-eval.md 路径），随 `67bfad2` 一并落仓。
5. **脚本不受影响确认**：bootstrap / deploy / drift-check / regression / ci-checks / fetch-binaries / motto 均以 `REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"` 相对路径定位，改名后零改动。

## 四、验收结果

- **本地**：drift-check PASS；regression 10 passed, 0 failed（含 deploy 收敛后的 drift）；bootstrap.sh 在改名路径下幂等重跑通过（binary 跳过 + shim 重建）；MIGRATION 按新 URL 走查无死步（clone URL 可解析、沙盒 bootstrap + drift PASS）。
- **CI**：见上表，末态双绿。
- **遗留观察**：`~/.pi/agent/check.sh` 步骤 2/5–4/5 引用 `notes/*.live.mjs` 与 `notes/*.test.mjs`，这些文件在省并时已归档至 `notes/archive/`——为**既有陈旧引用**（先于本工单），不在本次改齐范围，另议。

## 五、Commit 清单

| 仓 | commit | 内容 |
|---|---|---|
| `Motto` | `67bfad2` | 口径改齐：仓名 motto-extensions → Motto + clone URL 统一 + 两条编者注（单独一枚） |
| `Motto` | `b06108b` | regression：drift-check 无部署位即跳过（CI runner 环境差异修复，不改门槛） |
| `Motto` | `c4b5884` | README 快速开始补 clone URL |
| `~/.pi/agent` | `d0a8c11` | 部署位引用 ~/Projects/motto-extensions → ~/Projects/Motto |
