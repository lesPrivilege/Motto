# Motto 系列收官简报（五件归仓 · 发凡起例）

> 日期:2026-08-08
> 范围:收官单三阶段(统一验收含 TPS / 五件收敛入仓 / 常态节奏定稿)
> 基线:Pi 0.84.1;`motto-extensions`(canonical)与 `~/.pi/agent`(部署位)两仓

## 一、阶段一:统一验收(全绿)

- 全量门槛复跑:`./scripts/regression.sh`(9/9)+ governance(含 drift)PASS;
  48/48 单测(review-flow 25 + motto 15 + canonical-copy 8)+ 三 typecheck + 列宽 40/60/66/80/200 零超宽。
- 真实 pi 冒烟(部署位,健壮版驱动 17b61ec):三件加载 + `/copy-answer`(整段)+ `/copy-code`(fenced 原文)
  + turn.v1 落盘 PASS;并行乱序 + Ctrl+O + TPS 活体全 PASS。
- **TPS 专项**(新扩展,走 motto/TUI pack):流式滚动(实探 ~6→~229 t/s)/ 结算转均值(live 观测 71 t/s)/
  工具期分母不涨 / 窄宽按序被弃 / 无 NaN·∞,六项单测 + 活体全绿;与 f186ed9 比对逐项一致,差异仅 TPS 新增。
- 关键勘误:`message_update` 的 message 流式期为空对象(role 缺失)→ TPS 接线改按 assistantMessageEvent
  取 delta(单测 + 活体锁定)。

## 二、阶段二:五件收敛入仓

| Pack | 层 | 部署位 | 测试 |
|---|---|---|---|
| `motto` | TUI 品牌层(牌记 / footer 含 TPS / 标题 / 品牌化) | `extensions/motto/` | 15/15 |
| `motto-canonical-copy` | 复制边界层 | `extensions/motto-canonical-copy/` | 8/8 |
| `motto-review-flow` | 对话流 recap 层 | `extensions/motto-review-flow/` | 25/25 |
| `motto-themes` | 主题三 JSON | `themes/`(pi 主题目录,与扩展位分开) | 声明式 |
| `motto-computer-use` | 外部设备层(v1.0.0 tag 未动) | 自取(checksums) | 5/5 |

- 归仓形态:index.ts(薄接线)+ core.ts(纯逻辑);canonical source 唯一在仓库。
- 部署选型:**拷贝**(非 symlink),理由:diff 型 drift check 需要拷贝、jiti 对符号链接目录解析脆弱、
  部署位自包含对仓库迁移免疫。`scripts/deploy.sh` + `scripts/drift-check.sh`(diff 非空即报警)并入
  regression.sh 与 ci-checks.sh governance。
- 验收材料拆分入各 pack reports/(原件留 `.pi/agent/notes`);凡例正典 `docs/MOTTO.md`,
  `.pi/agent/MOTTO.md` 只留指针,不留两份可漂移正文。
- 裁定保留项(hardWrap/truncateToWidth 跨件重复、dev 脚本 dist theme 导入、Ghostty 终验等)
  原样登记各 pack README 遗留节,未趁迁移改。
- 适配项(非保留):`listExtensionNames` 增目录 pack 支持(归仓形态必要)。

## 三、阶段三:常态节奏定稿(docs/MAINTENANCE.md)

- dogfooding 优先:无 usage-log 不升级不扩面(五件统一)。
- 定期省视:**每双周或 pi 升级触发(取先到,写死)** = drift check + regression +
  上游 dist 导出核对(appendEntry/registerEntryRenderer)+ usage-log backlog 逐条裁定,产出入模板。
- Pi 升级流程写死:核对导出 → 部署位灰跑 → 全量门槛 → 仓内登记版本(不通过维持固定并记录)。

## 四、Commit 清单与两仓互引

| 仓 | commit | 内容 |
|---|---|---|
| motto-extensions | `b1107bb` | 五件归仓 + 治理(deploy/drift/regression/ci) |
| motto-extensions | *(本批)* | MAINTENANCE.md 收官节奏 + 本简报 |
| .pi/agent | `14683d0` | 部署位收尾(指针 + 旧源移除 + 镜像排除) |
| .pi/agent(源历史) | `e81cf08`/`687d0d7`/`a9bbba6`/`17b61ec`/`f186ed9`/`f6f93ca` | 补验 / 清理 / 阶段三 / 健壮版驱动 / s002 核验 / TPS |

## 五、遗留(预期)

1. **Ghostty 目视终验**(用户侧):双宗切换、Ctrl+O、标题行为、window-theme 一致性。
2. **CI 真实 runner 待 remote**:ci.yml/upstream-pin-verify.yml 本地验证通过,仓库尚无 remote,
   未在真实 runner 执行(ci-checks.sh 为 CI 等价验证)。
3. **4 项裁定保留项**:hardWrap/truncateToWidth 跨件重复、dev 脚本 dist theme 导入、Downloads 原始 GPT 包、
   真实模型并行行为依赖模型(已由 loader 活体覆盖乱序逻辑)。
