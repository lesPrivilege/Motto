# 全线省并记录 — 2026-08-08（收官省视）

> 日期：2026-08-08
> 对象：`motto-extensions` 仓全部 docs 与 pack 文档；`~/.pi/agent` 仓 notes 与配置；两侧临时残留
> 原则：结论入正典、过程入档案、临时物删除；省并只搬运与删除，不产生任何新论断
> 模板：`docs/templates/ACCEPTANCE.md`（省视专用节）

## 1. 文本省并（motto-extensions）

### 1.1 过程性旧措辞核对（四项，逐处核对）

| 旧措辞 | 核对结果 | 处置 | 出处（勘误 commit） |
|---|---|---|---|
| computer-use「显式 -e 加载」旧姿态 | 根 `README.md`「默认不启用，由用户在 Motto 侧显式加载」漏网 | **改齐**为「默认加载、门禁 fail-closed」（pack 经 deploy.sh 默认加载；computer-use 默认加载但会话级门禁默认关闭，须 `/computer-use approve`） | `db25f69`（默认加载 + 会话级显式门禁 armed fail-closed）、`f9f0e18`（纳入默认加载部署治理） |
| fail-closed「抛错」旧说法 | repo 内仅存于验收报告（历史记录，PACK-1/PACK-2 均为当次验收原样）；`docs/MOTTO.md` 三.9「不抛错」为静默失活的现行相反语义；其余 canon 用「fail-closed 门禁/拒绝并指引」 | 无需改（报告不改史） | `db25f69`（未批准抛错含 `/computer-use approve` 指引，不静默降级） |
| "review line" 旧名 | 全仓零残留（recap/review-safe 对象列已为现行名） | 无需改 | — |
| 三层 progressive disclosure 旧称 | 全仓统一「真三层 progressive disclosure（per-entry ID + collapsed/preview/full）挂上游 transcript projector」 | 无需改 | — |

### 1.2 正典互引核对（MOTTO / AGENTS / ROADMAP / MAINTENANCE）

- **无双份可漂移文本**：`docs/MOTTO.md` 为唯一正典，部署位 `~/.pi/agent/MOTTO.md` 只留指针；各 pack 体例引用均指向仓内正典。✓
- **失效路径修复（三处）**：
  - `docs/MOTTO.md` 五.3「上游跟踪项」：`notes/upstream/(UPSTREAM-PROPOSAL.md / UPSTREAM-SELECTION-PROJECTION.md)` → `docs/research/(…)`。
  - `docs/ROADMAP.md`「已裁定不做」：`notes/review-flow-eval.md` → `docs/decisions/review-flow-eval.md`（裁定锚点）。
  - `extensions/motto-canonical-copy/README.md`：两处 `~/.pi/agent/notes/upstream/…` → `../../docs/research/…`。
- **跨仓 commit 引用**（`563be3a` / `ea8f5cd`，源仓 commit）：源仓 `~/.pi/agent` 仍在，非失效路径；「立卷时路碑」本就声明为历史快照。保留。
- **MAINTENANCE 引用**：`docs/reviews/`（本记录落点，已建）、`docs/templates/ACCEPTANCE.md|USAGE-LOG.md`、`scripts/{regression,drift-check,deploy,ci-checks}.sh`、`~/.pi/agent/settings.json` 全部在位。✓

### 1.3 reports/ 与 research/ 归位

| 材料 | 自 → 至 | 校验 |
|---|---|---|
| 上游设计稿 3 份（UPSTREAM-PROPOSAL / UPSTREAM-SELECTION-PROJECTION / SUBMISSION-7721） | `~/.pi/agent/notes/upstream/` → `docs/research/` | 逐字节一致 |
| review-flow-eval.md（裁定锚点） | `~/.pi/agent/notes/` → `docs/decisions/` | 逐字节一致 |
| CLOSING-2026-08-08.md（收官简报） | `docs/` → `docs/reviews/2026-08-08-closing.md` | 过程入档案 |

- **REGISTRY 一致性**：五 pack 状态与报告/usage-log 全部在位（motto/canonical-copy/review-flow/themes SHIPPED；computer-use SHIPPED（ACCEPTED WITH LIMITATIONS），PACK-1/PACK-2 齐备）。✓

### 1.4 usage-log backlog 保全（省并缺口修复）

`~/.pi/agent/notes/usage-log/2026-08.md` 两条真实使用记录未随归仓入 pack usage-log，直接归档即丢 backlog。已保真搬运（原文逐条保留，仅路径/正则无失真）：
- ENOENT 品牌污染 → `extensions/motto/docs/usage-log/2026-08.md`
- thinking 全文入流摩擦 → `extensions/motto-review-flow/docs/usage-log/2026-08.md`

## 2. 配置与残留省并（~/.pi/agent）

- **notes/ 全量分类**（`notes/archive/README.md` 为索引）：
  - 收编原件 11 份 → `notes/archive/`，首行加「已收编，存档」标注（4 份 `.test.mjs`、体例定稿、双终验、PACK-INTERACT-1、TPS 验收、theme 闭环、usage-log）。
  - 过程草稿 9 份 + verify-thinking.py（CoT 核查证据脚本，随 `CoT-fold-verification.md` 证据链归档）→ `notes/archive/`。
  - 上游 3 份 + review-flow-eval.md → 已收编入 motto-extensions 仓（见 1.3），本仓 git rm。
- **settings.json**：无试验残键。`hideThinkingBlock` / `quietStartup` / `lastChangelogVersion` / `defaultThinkingLevel` 均经核对为 pi 0.84.1 `settings-manager` 原生键。
- **~/.zshrc**：仅两条 `MOTTO_COPY_ANSWER_SHORTCUT` / `MOTTO_COPY_CODE_SHORTCUT`，符合「只余两条」。
- **MOTTO.md 指针**：指向 `~/Projects/motto-extensions/docs/MOTTO.md`，在位；**格言文件** `motto`（慎厥身修思永）在位。
- **临时物删除**：`/tmp/motto-*` 沙盒与截图 19 处、`~/.pi/agent/.live/captures` 与 `.live/sessions`、`/private/tmp/tmux-501/{default,motto-live,motto-probe}` 陈旧 socket（均无 server 运行）、`notes/__pycache__/`、`.live/tmux` 空目录。

## 3. 终态验收

- **drift-check**：PASS（五 pack + 三主题部署位与仓库一致；pack 内文档变更经 `scripts/deploy.sh` 同步）。
- **regression**：`10 passed, 0 failed`（motto / canonical-copy / review-flow 单测 + computer-use gate/smoke/boundary/netcheck/proctree/permcheck + drift）。
- **真实 TUI 冒烟**（tmux 起 `pi`，cwd=`~`）：
  | 项 | 结果 | 证据 |
  |---|---|---|
  | 牌记含 `.motto/agent.md` | PASS | context 行 `AGENTS.md · .motto/agent.md`，` · ` 分隔、与 AGENTS.md 并列 |
  | recap（review-flow） | PASS | turn 投影 `1 tool · run 1 · 2.0s`（无标签计数起始、机械投影） |
  | copy（canonical-copy） | PASS | `/copy-answer` → pbpaste 命中最后回答原文；`/copy-code` 无 fenced block → fail-closed 未覆盖剪贴板 |
  | 门禁（computer-use） | PASS | `/computer-use status` → `armed: false (fail-closed until /computer-use approve)`；本会话 `cu_see` 未批准被拒含指引 |
  | footer TPS | PASS | 结算转均值 `12 t/s` 展示，`deepseek-v4-flash · max` 右簇在位 |

## 4. 悬置（记录待裁定，不当场改判）

1. **PACK-COMPUTER-USE-1-acceptance.md「默认不启用」历史措辞**：作为 PACK-1 验收原样保留（PACK-2 已载明姿态变更，REGISTRY/README/正典为现行）。是否回改历史报告以消除表面歧义 → 待裁定。
2. **docs/decisions/review-flow-eval.md 内历史路径**：「交付：notes/review-flow.test.mjs + notes/review-flow-eval.md」为 2026-08-07 原状；原件现于 `~/.pi/agent/notes/archive/` 与本仓 `docs/decisions/`。路径不追改，映射由 `notes/archive/README.md` 与本记录载明 → 待裁定是否补注。
3. 其余无新增语义冲突。

## 5. 终态盘点（多一件说明身份）

仓内现存即：**正典四件**（docs/MOTTO.md、AGENTS.md、docs/ROADMAP.md、docs/MAINTENANCE.md）、**pack 五件 + REGISTRY**（extensions/）、**档案四类**（docs/research/ 3 件、docs/decisions/ 2 件、docs/reviews/ 2 件、pack 内 usage-log）、**scripts 治理件**。此外：
- `README.md`（仓总纲/目录体例/五件归仓表）
- `CONTRIBUTING.md`（加入/更新 pack 流程）
- `docs/MIGRATION.md`（bootstrap.sh 迁移足迹清单，被 README 与 bootstrap 引用）
- `docs/templates/`（ACCEPTANCE.md / USAGE-LOG.md，被 MAINTENANCE 引用）
- `LICENSE` / `.github/` / 各 pack `checksums/`（治理与供应链）

## 6. Commit

| 仓 | commit |
|---|---|
| `motto-extensions` | （本批） |
| `~/.pi/agent` | `947d947`（notes 全线归档 + 临时物清除） |
