# usage-log — 2026-08

真实使用记录（dogfooding）。每条 = 一次真实工作流中的遭遇。这是维护 backlog 的来源；
**没有记录的功能不升级、不扩面**。

## 条目格式

```text
### YYYY-MM-DD HH:MM — <一句话现象>

- 场景：<这次在做什么真实任务>
- 扩展/动作：<pack> <cu_xxx / 命令>
- 目标应用/环境：<TextEdit / Safari / 多屏 / …>
- 现象：<成功 | 失败 | 异常行为 | 想做的做不到>
- 可逆性：<是否可逆；已如何恢复>
- 疑似根因：
- 处置：<已修 / 待修 / 记录为版本限制 / 已记录待验收>
- 跟进：<issue/回链接>
```

## 2026-08

### 2026-08-11 — 视觉观察契约收紧：provider-neutral canonical projection

- 场景：将视觉层从 Gemini-specific text passthrough 收紧为 text-native、provider-neutral observation seam。
- 扩展/动作：`motto-gemini-vision` `motto_vision`；新增 `contract.ts`、`GeminiAdapter` 与 fake-provider 注入测试。
- 目标应用/环境：离线 Node 测试；未调用 Gemini、DashScope 或其他外部多模态 API。
- 现象：成功。`ObservationRequest`/`ObservationResult`/evidence/artifact/provenance 均做 runtime validation；主模型只收到 deterministic projection，canonical result/provenance 在 details；raw provider JSON、key、base64 不泄漏；bbox/page/timestamp 越界 fail-closed；错误可区分 config/input/provider/quota/timeout/aborted/invalid_output。
- 可逆性：代码变更可由仓库镜像重新部署；未改 TUI 文档或根 fixtures。
- 疑似根因：旧实现把 provider text 直接当 tool content，缺少跨 provider 契约与可复核 locator。
- 处置：**已修并离线验收**。受限沙箱初跑为原 69 项中 66 PASS、3 项因 localhost/home 写入 `EPERM` 明确 NOT TESTED；补强 provider 异常与结果的敏感信息守卫后，授权本地复跑 `npm test` **71/71 PASS**，`npm run typecheck` PASS，未将沙箱限制降级为功能结论。pack regression 的 unit 阶段 PASS；因本轮明确不部署，drift-check 对仍为 PACK-VISION-1 的用户部署镜像报 expected FAIL，PACK-VISION-2 状态为 `DEPLOYMENT_PENDING`。
- 跟进：`reports/PACK-VISION-2-observation-contract.md`；若观察无法稳定文本化，改用原生多模态主模型 one-shot，不扩展本工具为长视频/多图循环。

### 2026-08-11 — 三张程序化视觉夹具的本地多模态人工验收

- 场景：验证纯文本 coding agent 是否能仅凭 canonical result + text projection 采取下一步，不调用 Gemini/DashScope 等外部 provider。
- 夹具：`ui-state.png`（Settings 表单、灰置 Save）、`ocr-text.png`（Release checklist OCR）、`spatial-layout.png`（左右红/蓝区域与方向箭头）；均在 `/private/tmp/motto-vision-contract-fixtures/` 程序化生成，不入仓、不改根 `fixtures/`。
- 人工观察：使用本会话 Luna 自身多模态能力查看，确认三图均清晰可读。UI 结果以 Save 的 normalized bbox + “仅可确认可见状态” limitation，足以指导 agent 先检查应用 dirty-state，而不会误断言可保存；OCR 结果保留版本、状态、owner、命令、note 与 page-region locator，足以指导 agent 执行 `npm run test` 前先 review；空间结果给出左右 bbox 与箭头方向，足以指导 agent 修改布局/断言方向。三次 projection 均为稳定 `Conclusion` + 非空 evidence（UI 另含 limitation）。
- 结论：**PASS（人工视觉契约验收）**。观察层能把 UI 状态、OCR 文本、空间定位压成纯文本且保持证据/限制；无法稳定文本化的重复视频/多图任务仍明确切换原生多模态主模型 one-shot。
- 可逆性：夹具为临时文件，已留作本轮人工记录，未进入仓库或部署位。

### 2026-08-09 01:44 — 部署后普通 Pi 会话自动发现 motto_vision

- 场景：将 pack 纳入仓库统一部署治理后，验证不带 `-e` 的普通 Pi 启动是否自动加载。
- 扩展/动作：`./scripts/deploy.sh motto-gemini-vision` → `pi -p` 正常启动。
- 目标应用/环境：真实用户级部署位 `~/.pi/agent/extensions/motto-gemini-vision/`；Pi 0.84.1。
- 现象：成功。普通 Pi 会话确认当前 toolset 可用 `motto_vision`；本次只验证发现/注册，没有调用视觉 API，避免额外账单。
- 可逆性：部署位是由仓库重新生成的镜像；未改动凭据，未写入 key、截图或 base64。
- 疑似根因：—。
- 处置：**已部署并回归**；`regression.sh motto-gemini-vision` 通过（2/2），drift-check PASS，governance PASS。
- 跟进：进入 SHIPPED（ACCEPTED WITH LIMITATIONS）维护态；日常 dogfooding 持续记录。

### 2026-08-09 01:37 — REAL USER DOGFOOD：真实桌面截图经完整 pipeline 读取成功

- 场景：用户在真实 Motto 会话中询问能否读取桌面 `Screenshot 2026-08-08 at 18.46.11.png`（1398×1600 真实截图）的内容；文本主模型（deepseek）经 pack 真实代码路径调用 Gemini 视觉，非 subagent、非预设问题。
- 扩展/动作：motto-gemini-vision：`runTool` → `loadImage` → `runVision`（完整 pipeline）。
- 目标应用/环境：真实 Google Interactions API（经本机代理 127.0.0.1:9090，`NODE_USE_ENV_PROXY=1`）；凭据经 `~/.pi/agent/auth.json` 的 `google` api_key 解析；Node v25.9.0。
- 现象：成功（一次成功 + 一次超时，均如实记录）。
  1. **首次尝试（超时）**：直接发送 689 KB 原图 → 30s 默认超时（`motto_vision request timed out after 30s`）——全分辨率截图请求超限。
  2. **重试（成功）**：`sips -Z 1600` 降采样至 1398×1600 + JPEG 85（346 KB）→ `MOTTO_VISION_TIMEOUT_MS=120000` → **completed，15.7s（15690ms），2016 tokens（input 1262 / output 754）**，模型 `gemini-3.6-flash`。非预设问题（完整描述截图内容）成功识别主要内容：Motto 窗口标题栏、正文 markdown 汇报（Vercel skill 安装详情）、底部状态栏各字段。
- 可逆性：临时目录与降采样副本已清理；部署为可由仓库重新生成的用户级镜像，凭据未写入仓库。
- 疑似根因：默认 30s 超时对全分辨率大截图偏紧；降采样后体积/耗时均大幅回落。
- 处置：**已记录**——本次为真实大图 + 真实 Motto 使用链路的代表性 dogfood（非 8×8 预设 smoke）；超时行为亦如实留档，不掩盖。
- 跟进：本条目支持 `LIVE_PROVIDER_VERIFIED` ✅、`PI_RUNTIME_DOGFOODED` ✅；部署后回归与 drift-check 已通过。未写入截图、key、base64 或完整 transcript。

### 2026-08-09 01:50 — Google 凭据统一真源迁移 + 隔离验证（auth.json → 统一 store）

- 场景：按「单一真源 + provider bridge」体例统一凭据：deepseek 已统一；google 从 `~/.pi/agent/auth.json` 迁至统一目录，并经隔离验证后删除 auth.json 副本。
- 扩展/动作：凭据基础设施（motto-credential / motto-google-key / models.json `!` 引用）+ motto-gemini-vision 读取链。
- 目标应用/环境：真实 pi 0.84.1 + 真实 Gemini；`~/.config/motto/credentials/{deepseek,google}`（0600）；本机代理 127.0.0.1:9090。
- 现象：成功。
  1. **隔离验证 PASS**：`PI_CODING_AGENT_DIR` 指向空 auth.json 的临时目录，google 仅经 models.json `!/…/motto-google-key` → `getApiKeyForProvider("google")` 真实调用 Gemini 成功（返回 Red）→ 证明 `!` 引用可被 modelRegistry 解析（无 stored credential 时走 ambient）。
  2. 真机切换后（models.json 加 google provider `!motto-google-key`；auth.json 移除 google）`pi auth check --provider google` → ready；fresh pi 部署位闭环返回 "Red"；live smoke（读统一 store）PASS。
- 可逆性：auth.json 备份 /tmp/auth.json.bak；统一目录文件与旧源哈希一致（迁移复制，未丢）。
- 疑似根因：—（体例演进，非缺陷）。
- 处置：**已记录**——auth.json 不再持有 google；如需回退可恢复 /tmp/auth.json.bak（此时 stored credential 会优先于 `!` 引用）。
- 跟进：DeepSeek 统一于上一轮完成；Google 引用链本轮完成；`~/.config/deepseek/api-key` 旧副本已删（备份 /tmp/deepseek-api-key.bak）。

### 2026-08-09 01:20 — live provider 验证通过 + 真实 Pi runtime dogfood 闭环（Gate 1+2 完成）

- 场景：凭据经 pi `/login` 本地凭据管理接入后（`~/.pi/agent/auth.json`，gitignored，key 全程未入仓/未回显），执行真实验证。
- 扩展/动作：motto-gemini-vision：`test/live.mts` + `pi -p -e ./extensions/motto-gemini-vision/index.ts`。
- 目标应用/环境：真实 Google Interactions API（经本机代理 127.0.0.1:9090；standalone 需 `NODE_USE_ENV_PROXY=1`，pi 进程内用 pi 的全局 proxy dispatcher 免设）；Node v25.9.0。
- 现象：成功。
  1. **live smoke PASS**：真实 8×8 红 PNG → gemini-3.6-flash，status=completed，durationMs=2729，usage={total_input_tokens:1245,total_output_tokens:1,total_tokens:1246}，模型回答 "Red"。
  2. **真实闭环 dogfood PASS**：`pi -e` 下主模型自主调用 motto_vision（16×8 左红右蓝 PNG）→ 真实 Gemini 返回左右颜色 + 边界观察 → 文本 tool result → 主模型消费并继续作答；pi 会话文件经扫描无 key/base64。
- 可逆性：全部可逆（临时目录清理；无部署、无仓库提交）。
- 疑似根因/已修：首版 live 返回 HTTP 400——真实 v1 API 不接受顶层 `input[]` 直接 `{type:"image"}`；正确形状为单个 `{type:"content", content:[text,image]}` 消息。**已修**（buildRequest + 断言更新），live PASS 复核。
- 处置：**已部署并回归**——`LIVE_PROVIDER_VERIFIED` ✅、`PI_RUNTIME_DOGFOODED` ✅、`DEPLOYED_AND_REGRESSED` ✅。
- 跟进：进入 SHIPPED（ACCEPTED WITH LIMITATIONS）维护态。

### 2026-08-08 22:05 — 无 key 真实 harness runtime loading smoke（pi -e，非真实 dogfood）

- 场景：结案前降低外部风险 #4/#7——在真实 pi harness 中验证 extension 可被发现、注册、路由与 fail-closed，无需 key。
- 扩展/动作：motto-gemini-vision：`pi -p -e extensions/motto-gemini-vision/index.ts`，提示主模型调用 `motto_vision`。
- 目标应用/环境：真实 pi 0.84.1（print mode），同机 ~/.pi/agent 配置，无 `GEMINI_API_KEY`。
- 现象：成功。主模型确认工具已注册（toolset 中有 `motto_vision`）并实际发出工具调用；在 pi 本地 Google credential 缺失时工具立即 fail-closed，返回标准 tool error `motto_vision: Google Gemini API key is not configured…`（且 key 检查先于路径检查，验证了 fail-fast 顺序）；主模型消费错误并继续作答；motto-computer-use 等其余 Motto extension 正常加载/退出，无冲突。
- 可逆性：只读验证，零副作用（未部署、未写部署位）。
- 疑似根因：—
- 处置：**已记录**——本条目为 `RUNTIME_LOADING_SMOKE_VERIFIED`（发现/注册/路由/fail-closed），**不关闭** `PI_RUNTIME_DOGFOOD_PENDING`（真实 Gemini 调用、模型自主选择时机、toolResult 闭环、transcript 无 base64 均需 key 后一次性验证）。
- 跟进：Gate 2（pi -e 真实闭环）待 GEMINI_API_KEY。

### 2026-08-08 21:40 — motto-gemini-vision 首版离线验收 + provider-stub 集成 dogfood（PACK-VISION-1）

- 场景：实现 `motto_vision` 工具（工作单：按需 Gemini 视觉，非 subagent）。按项目本地正文
  末注「Agent 本身 dogfooding 解耦实现后再写入 Motto」的约定：先以 Agent 自身驱动真实代码
  路径做端到端集成验证，再落验收与注册。
- 扩展/动作：motto-gemini-vision：`runTool` → `loadImage` → `runVision` → 真实 `fetch`。
- 目标应用/环境：**本地 provider stub**（127.0.0.1 HTTP 替身，模拟 Gemini Interactions API）+
  真实 8×8 红 PNG fixture；Node v25.9.0；无 standalone `GEMINI_API_KEY`（Pi 本地 credential
  由 runtime smoke 单独覆盖）。
- 现象：成功。61 项离线测试全绿；provider-stub 集成（test/dogfood.test.mjs）将固定生产
  endpoint 重定向到本地替身、工具代码路径逐字节运行：真实图片字节 → 真实 HTTP POST → 真实
  响应解析，服务端核验了 `store:false`、input 顺序、MIME、base64 可解码回原字节、key 仅现于
  `x-goog-api-key`。
- 可逆性：全部可逆（临时目录自动清理；无任何外部副作用）。
- 疑似根因：—（首版验收，无缺陷发现）。
- 处置：**已记录待验收（live）**——本条目是 **local provider-stub integration dogfood**，
  不是真实 Gemini 调用，也不是真实 Pi runtime dogfood。以下状态明确 pending：
  - `LIVE_PROVIDER_PENDING`：真实 Google 请求（`test/live.mts`，8×8 红 PNG + 颜色定点提问）
    因当前环境无 `GEMINI_API_KEY` 未执行；
  - `PI_RUNTIME_DOGFOOD_PENDING`：尚未经 `pi -e` 在真实 agent loop 中由文本主模型自主调用
    `motto_vision` 并闭环（工具选择 / 定点提问 / toolResult 入上下文 / 主模型继续 / transcript
    无 base64）；
  - `DEPLOYMENT_PENDING`：未加入 deploy.sh，未部署到 `~/.pi/agent`。
- 跟进：`reports/PACK-VISION-1-acceptance.md`（结论 ACCEPTED WITH LIMITATIONS）；状态机：
  ACCEPTED WITH LIMITATIONS → (live 通过) LIVE_PROVIDER_VERIFIED → (pi -e 真实任务)
  PI_RUNTIME_DOGFOODED → (部署+回归) SHIPPED。
