{
  "name": "motto-review-flow",
  "version": "0.1.0",
  "private": true,
  "description": "Conversation recap layer: per-turn tool review projection (motto-review-flow.turn.v1 custom entry), pure display, never in model context.",
  "type": "module",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "@earendil-works/pi-tui": "0.84.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  }
}
