# usage-log — 2026-08

真实使用记录(dogfooding)。**没有记录的功能不升级、不扩面**。

## 条目格式

```text
### YYYY-MM-DD HH:MM — <一句话现象>
- 场景 / 扩展·动作 / 环境 / 现象 / 可逆性 / 疑似根因 / 处置 / 跟进
```

## 2026-08

### 2026-08-09 — PACK-THEMES-2 回退取消,恢复 60d24c9 dim 状态(REINSTATED)

- 场景:工单 CORRECT-ROLLBACK-SCOPE-1 裁定 rollback 范围过宽——PACK-THEMES-2 系独立于 fenced caption 的既有标题视觉工单,三主题 mdHeading 改回 text 属连带回归(bab176c 已认定 text 为「与正文同色、层级不可辨」基线)。
- 扩展/动作:motto-themes——三主题 mdHeading 由 text 恢复为 dim(精确还原 commit 60d24c9 功能状态,blob 逐字节一致,不发明新方案)。
- 环境:Pi 0.84.1;macOS 26.5.2。
- 现象:回退草稿文字(报告 §7 / 本 log 回退条目 / REGISTRY ROLLED_BACK 标注)因未 commit 未成历史,按裁定直接撤销,不追加「勘误的勘误」;原 60d24c9 报告与历史记录不动。
- 处置:恢复 dim + 部署位同步 + 门禁复核;FLOW-FENCED-BLOCKS-1 回退继续保留(状态不变)。
- 状态登记:**PACK-THEMES-2 — ACCEPTED(REINSTATED,真实 TUI 多级样本目验通过 2026-08-09)**。
- 目验:fresh pi(scratch 目录,无旧缓存)真实 Ghostty TUI,完整多级样本单消息回显——H1 无 # 前缀 bold+underline、H2 无 ## 前缀 bold、H3 保留 ### 前缀 bold,三档同色 dim(#a8adb2)且与正文(#f2f3f4)有色差;text/log/bash fence 无 caption;canonical/session 存原始 markdown 零改动。用户目验确认「已实现 H1 H2 区分,符合期待」。
- 跟进:已提交推送(见仓根 commit);上游逐级槽仍为 ROADMAP 候选(保持挂账)。

### 2026-08-08 — mdHeading 明度分层小单落单(mdHeading: text → dim)

- 场景:前条触发候选成案后按核查记录第四节规格出小单,三主题 mdHeading 由正文字色改为 dim。
- 扩展/动作:motto-themes(三 JSON 各一处 mdHeading → dim);报告 PACK-THEMES-2-acceptance.md。
- 环境:Pi 0.84.1;macOS 26.5.2。
- 现象:成功。深宗 dim(#a8adb2)亮于 mid 一档、浅宗 dim(#5c6166)深于 mid 一档,对比度 6.54:1/5.63:1(>4.5:1);浅宗 dimmer 1.70:1 未触碰不回归;regression 10/10 + drift PASS。
- 可逆性:可逆(纯主题取值,改回 text 即还原)。
- 处置:已交付(单 commit);双宗样张见报告第 3 节。
- 跟进:上游逐级槽候选保持 ROADMAP 挂账不动;Ghostty 目视终验用户侧遗留。

### 2026-08-08 — chat 区 heading 以原始 `###` 呈现,层级不可辨(触发候选)

- 场景:收工 review 屏(assistant 消息 markdown)内多级 heading 混排,层级无法一眼分辨;h3+ 只见原始 `### ` 字样,与正文区分弱。
- 扩展/动作:motto-themes(mdHeading 槽);对象 pi-tui Markdown 渲染器 + theme-schema。
- 环境:Pi 0.84.1;macOS 26.5.2;motto/dark/light 三主题同值。
- 现象:核查定性——**主题可调成案**。渲染器 heading 保留 `###` 前缀是设计(h1/h2 无前缀,靠 bold/underline;h3+ 靠 `#` 数量);主题面仅暴露单一 `mdHeading` 槽,motto 三主题当前全为 `"text"`(与正文同色),而 pi 默认主题为黄/橙——motto 把 heading 设成正文色是层级不可辨的直接根因。
- 可逆性:可逆(纯主题取值,改回 `"text"` 即还原)。
- 处置:核查记录入 `docs/research/MOTTO-MARKDOWN-HEADING-2026-08-08.md`;档一(motto-themes 小单,mdHeading 上色)成案待裁定执行,档二(上游逐级槽)挂跟踪清单,档三不适用。
- 跟进:主题小单规格见核查记录,待用户裁定后另开工单;上游逐级槽量级小,可作友好 PR 候选。

### 2026-08-08 — 归仓首记(五件收敛入仓)

- 场景:收官单五件归仓,本 pack 迁入 motto-extensions(唯一 canonical source),部署位由 scripts/deploy.sh 统一拷贝。
- 扩展/动作:motto-themes 归仓 + 验收材料拆分入 reports/ + 体例引用 docs/MOTTO.md。
- 环境:Pi 0.84.1;macOS 26.5.2。
- 现象:成功。单元测试/typecheck 全绿;真实 pi 冒烟/活体通过(证据见 reports/ 各验收报告)。
- 可逆性:可逆(源仓 .pi/agent 保留历史与验收原件)。
- 处置:已收编。
- 跟进:Ghostty 目视终验(用户侧);后续改动一律先本 log 留痕再动。
