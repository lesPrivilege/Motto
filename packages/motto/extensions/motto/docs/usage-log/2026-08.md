# usage-log — 2026-08

真实使用记录(dogfooding)。**没有记录的功能不升级、不扩面**。

## 条目格式

```text
### YYYY-MM-DD HH:MM — <一句话现象>
- 场景 / 扩展·动作 / 环境 / 现象 / 可逆性 / 疑似根因 / 处置 / 跟进
```

## 2026-08

### 2026-08-09 — 多级标题视觉投影:H3–H6 投影为 `## › 标题`(display-only,MD-HEAD-3TIER-1)

- 场景:工单「TUI 多级 Markdown 标题视觉投影」——常见 assistant 输出中 H3–H6 前成堆的 `#`
  (H6 显示 `###### `)使层级不可辨;目标为常规 chatbot 三层视觉(H1/H2/H3–H6 统一 `› 标题`),
  不显示标题井号、不做真六级样式、不动主题 token、不改 canonical。
- 扩展/动作:motto `registerMarkdownTransformer(projectDeepHeadings)`(模块 headings.ts,
  纯函数,小逐行 scanner;H3–H6 → `## › 原标题`,仅 assistant 完成态)。
- 目标应用/环境:Pi 0.84.1;macOS 26.5.2;真实 pi TUI(tmux 嵌套 pty,fresh 自动发现 motto)。
- 现象:成功。22 项单测全绿(含真实 pi-tui renderer 输出级断言:`### 三级标题` 投影后渲染为
  `› 三级标题` 且不含 `### 三级标题`)+ typecheck + 全量回归 + governance PASS;真实 TUI dogfood:
  H1 无前缀 bold+underline、H2 无前缀 bold、H3–H6 统一 `› 标题`(bold,dim #a8adb2,无井号);
  ```python 围栏内 `###`/`######` 注释行逐字不动;canonical/session 存原始 markdown 零改动。
- 可逆性:可逆(展示层纯函数,不落 session;移除注册即回原生 H3–H6 前缀;基线 H1/H2、
  `mdHeading: dim`、PACK-THEMES-2 定本均未触碰)。
- 疑似根因:—(新能力)。首版实现误把规则做成 H4–H6 钳制为 H3(保留 `###` 前缀),
  dogfood 目验未达「无标题井号」目标,已按纠偏口径重做为 H3–H6 → `## ›`。
- 处置:已实现 + 重跑验收 + **用户真实 Motto/Ghostty TUI 目验通过(2026-08-09,截图确认)**
  → PACK-MOTTO-3 记 ACCEPTED(见 reports/PACK-MOTTO-3-acceptance.md);已部署(deploy.sh motto)
  供日常狗粮。本批随 commit 推送远端。
- 跟进:与 ROADMAP「mdHeading 逐级槽」(theme 侧逐级取色,上游解锁候选)不冲突、互不替代;
  list 内嵌套标题不做(须递归解析,超出最小面);blockquote 前缀 heading 已投影。

### 2026-08-09 — FLOW-FENCED-BLOCKS-1 目验复核:视觉目标失败,状态撤销并回退(ROLLED_BACK)

- 场景:用户于真实 Motto/Ghostty TUI 对 fenced 块展示牌记做最终目验——决定性截图证据:captain
  transformer 与 Pi Markdown renderer 均已实际加载,但视觉结果没有形成 card affordance;caption 只是
  普通正文,原生 renderer 仍显示 ```lang / ```,H3+ 仍显示 ### 前缀,整体只增加纵向留白未改善
  reviewability。此前 FLOW-FENCED-BLOCKS-1 的 ACCEPTED 判定撤销,执行既定 NO-GO 回退。
- 扩展/动作:motto——移除 index.ts 中 annotateFencedBlocks import 与 registerMarkdownTransformer 注册;
  删除 fenced-blocks.ts 与 test/fenced-blocks.test.mjs;tsconfig include 恢复 [index.ts, core.ts]。
- 环境:Pi 0.84.1;macOS 26.5.2;真实 Motto/Ghostty TUI(fresh 自动发现扩展)。
- 现象:技术链路通过(transform 实际加载),但视觉目标失败(caption 无 card affordance、fence 仍原生、
  H3+ 仍 ### 前缀、只加留白)。回退后:fenced 块回到 Pi 原生基线,canonical/session
  无任何迁移或改写。
- 可逆性:回退即原状(展示层纯函数已整体移除)。
- 疑似根因:展示层 caption 不足以达成「显式化 fenced 块」的视觉目标,属方案否决而非技术缺陷。
- 处置:已回退 + 部署位同步 + 全量门禁通过(见报告 PACK-MOTTO-2-acceptance.md 第 9 节勘误)。
- 状态登记:**FLOW-FENCED-BLOCKS-1 — ROLLED_BACK (VISUAL_ACCEPTANCE_FAILED)**。
- 跟进:push 前等待用户对回退后真实 TUI 基线截图确认;不扩大实现面(不造 card renderer/Unicode 伪 card)。

### 2026-08-09 02:05 — FLOW-FENCED-BLOCKS-1:fenced 块展示牌记落地(display-only)

- 场景:工单「显式化 fenced text/bash 块」——完成态、顶层、带 allowlist 语言标记的 fenced block
  在 TUI 中于 opening fence 上方投影轻量 caption(文本块/日志/命令片段 · N 行)+ 空行。
- 扩展/动作:motto `registerMarkdownTransformer(annotateFencedBlocks)`(新模块 fenced-blocks.ts,纯函数)。
- 目标应用/环境:Pi 0.84.1;macOS 26.5.2;真实 pi TUI(嵌套 pty + tmux capture)。
- 现象:成功。21 项单测全绿 + typecheck + 全量回归(11/11)+ governance PASS;真实 TUI dogfood:
  assistant 三块(text→`文本块 · 3 行`、bash→`命令片段 · 2 行`、json 不加)、user text fence、
  边界(未闭合/unsupported/reload/resize/resume)均符合验收;canonical session 正文无 caption。
- 可逆性:可逆(展示层纯函数,不落 session;移除注册即回原样)。
- 疑似根因:—(新能力,非缺陷)。
- 处置:已实现 + 验收(见 reports/PACK-MOTTO-2-acceptance.md);已部署(deploy.sh motto)供日常狗粮。
- 跟进:另流 `motto-gemini-vision`「凭据统一真源」改动与本工单无关,未随本批提交;远端同步见仓根 commit。

### 2026-08-08 — 项目本地域立域 + 拓扑封闭入宪(.motto/agent.md 注入)

- 场景:工单「.motto 项目本地域立域 + 拓扑封闭入宪」(行为变更,写者/验收分离);续单收尾含命令位注册(见仓根 commit)。
- 扩展/动作:motto `before_agent_start` 注入 cwd `.motto/agent.md` 为独立段(身份段之后,项目原文逐字节保留);超 32KB 截断+标注+每会话 notify 一次;牌记 `context` 行与 AGENTS.md 并列列出。
- 环境:Pi 0.84.1;macOS 26.5.2。
- 现象:立域依据 = 正典内容触发(所有者裁定最终拓扑);宪制第 2 条「确有需要」由 `.motto/agent.md` 的设立而成立;单测四判定(存在/缺失/超限/次序稳定)全绿;含 `.motto` 项目活体:牌记 context 行显示、模型可复述正文口令;无 `.motto` 项目零差异。
- 可逆性:可逆(扩展只读消费,不建目录不写文件;移除 `.motto/agent.md` 即回零注入)。
- 处置:已立域入宪 + 行为落地。
- 跟进:宪制新增拓扑封闭(不设第三仓)与项目本地域两款;docs/MOTTO.md 补「六、项目本地正文」;命令位 `scripts/motto` 见同批 commit。

### 2026-08-08 — 归仓首记(五件收敛入仓)

- 场景:收官单五件归仓,本 pack 迁入 motto-extensions(唯一 canonical source),部署位由 scripts/deploy.sh 统一拷贝。
- 扩展/动作:motto 归仓 + 验收材料拆分入 reports/ + 体例引用 docs/MOTTO.md。
- 环境:Pi 0.84.1;macOS 26.5.2。
- 现象:成功。单元测试/typecheck 全绿;真实 pi 冒烟/活体通过(证据见 reports/ 各验收报告)。
- 可逆性:可逆(源仓 .pi/agent 保留历史与验收原件)。
- 处置：已收编。
- 跟进：Ghostty 目视终验（用户侧）；后续改动一律先本 log 留痕再动。

### 2026-08-08 16:57 — 观察期首条代码缺陷：品牌化替换越界污染 skill 路径（ENOENT）

- 场景：reading-companion skill 触发，按系统提示词内给出的 SKILL.md 位置读取，ENOENT——
  路径被 motto 扩展品牌化改写：`~/.pi/agent/skills/...` → `~/.Motto/agent/skills/...`。
- 扩展/动作：motto（before_agent_start 品牌化——全文正则 `/(?<![\-\w])pi(?![\-\w])/gi` → "Motto"）。
- 目标应用/环境：pi 0.84.1；motto pack 0.1.0。
- 现象：提示词内 `<location>…/.Motto/agent/skills/…</location>`，模型按污染路径读取失败。
- 可逆性：代码缺陷，可修可回归锁定。
- 分类：观察期预设三类之外的**首条代码缺陷记录**（非 UI 摩擦、非上游边界）。
- 疑似根因：正则在词边界之外只排除 `-`/词字符，`.` 与 `/` 未排除 → `.pi`、`/pi`
  被改写为 `.Motto`、`/Motto`。功能 token（路径）被品牌化改写。
- 处置：**放弃全文替换，只注入身份段**——`injectBrandIdentity`（上游原文逐字节不动 + 末尾
  拼接身份段）；单测锁定（含 `.pi` 路径 / `pi-subagents` 包名 / `@earendil-works/pi-*` 前缀样本，
  断言逐字节不变）；活体通过（真实 pi 读 reading-companion SKILL.md 成功）。
- 裁定依据：**extension 与 fork 的边界以功能性内容为界**——品牌只做加法（注入身份段），
  不改写上游提示词原文；「设计语不外泄」的对偶条款「功能语不可侵」已入凡例
  （`docs/MOTTO.md` 总纲五.5）。
